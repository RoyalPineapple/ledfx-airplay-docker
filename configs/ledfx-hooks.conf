# LedFx AirPlay Hook Configuration
# This file configures which LedFx virtuals should be controlled by AirPlay session hooks

# Comma-separated list of virtual IDs to control
# Example: VIRTUAL_IDS="virtual-1,virtual-2"
# Leave empty or unset to control all virtuals
VIRTUAL_IDS=""

# LedFx API connection
LEDFX_HOST="localhost"
LEDFX_PORT="8888"

